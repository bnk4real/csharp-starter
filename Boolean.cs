class Boolean
{
    public static void BooLean()
    {
        bool iamRich = true;
        bool iamPoor = false;
        Console.WriteLine(iamRich);   // Outputs True
        Console.WriteLine(iamPoor);   // Outputs False

        // or else
        int x = 10;
        int y = 9;
        Console.WriteLine(x > y); // returns True, because 10 is higher than 9
    }
}